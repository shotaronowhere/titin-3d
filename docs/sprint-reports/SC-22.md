# SC-22 handoff

- Starting commit / ending commit: `349a27333e35f1fb6e7a2a87119ebbe98044ce1b` / uncommitted
  working tree based on the same commit. No commit was created because the owner did not request one.
- Consumed decision IDs and fingerprints: SD-01 `APPROVED`, SD-02 `DEFERRED`, SD-03
  `APPROVED`, SD-04 `APPROVED`, and SD-05 `APPROVED`; implemented model fingerprint
  `7badc8e270e73e8bae3d84420448e6c79fee9e41bfdb0ca790484750ef329ef6`; SD-04
  parameter set `q8wz42-1-literature-bounded-force-law-SD-04-v2`.
- Status: **COMPLETE**. This describes SC-22 only and is not an MVP
  release claim; `release_ready` remains `false`.
- User-visible changes: selected structures, chapters, chart points, modeled output, and parameters
  now route to canonical evidence; the Guided inspector is compact; Evidence has one full-detail
  owner; the current force-chart point is keyboard/click selectable; Close restores focus to the
  visible outer invoker.
- Scientific/data changes and reviewer authority: no scientific ruling, claim statement, support
  relation, model parameter, or model input was changed by SC-22. The UI consumes the
  project-owner-authorized, citation-backed SD-04 ruling. No independent human expert review is
  claimed.
- New or changed interfaces/schemas: `claimViewModel(claimId, context)` remains the only pure
  ClaimView export; each `ClaimField` now includes its canonical `claimId`; `renderClaimView` remains
  the sole DOM renderer and exposes the binding through `data-claim-id`; force-parameter source
  context now carries `claim_id: force_law_parameter_set`; source precedence remains value → object
  → chapter → all.
- Tests written first and observed failure: the remediation tests were added before the fixes. The
  first 11-test SC-22 run failed 6 cases: annotation PEVK copy leaked an unsupported phosphorylation
  statement, annotation ClaimViews used noncanonical copy, parameter source context lacked a claim
  ID, compact-inspector/chart bindings were absent, the governed performance baseline had been
  replaced by a dirty checkpoint, and this handoff lacked the required schema/counts/matrix. The
  first new browser run also failed because chapter 3 already selected PEVK and the test exercised
  its toggle-off path; the test now deliberately moves away and back before measuring the pinned
  card. A final source-boundary regression then exposed expert finding statuses being reduced to
  `UNKNOWN` in the pure view model and repaired after DOM rendering; finding status is now validated
  and resolved in `ClaimView`, with the renderer limited to presentation. The same audit found that
  two expert cards and three chapters lost declared contextual citations; fields now retain their
  source IDs and presentation surfaces use the exact union of canonical support and declared
  contextual sources.
- Negative controls: `scripts/neg_control_sc22.py` substitutes the valid but unrelated `PDB:6KN7`
  reference into an approved titin-continuity fixture and requires the reviewed digest to fail. It
  also removes one annotation's claim-support IDs and requires the missing public binding to fail.
- Commands run and results: `npm run build` PASS (standalone 1,987,197 bytes); `npm run pack`
  PASS (15 generated outputs); `npm run verify:sc22` PASS (42/42 bounded Node tests plus both
  annotation/presentation/claim validators and both negative controls); `npm run verify` PASS
  (528/528 Node tests, every negative/destructive control and validator, generated-artifact checks,
  the 52-cell matrix, and the Python structure smoke); `npm run test:browser` PASS (18/18 Chromium
  tests); `npm run test:browser:evidence` PASS (9/9 Chromium tests). One consecutive intermediate
  Evidence-suite invocation hit a non-reproduced Playwright actionability timeout on its first
  visible button; the isolated case passed 1/1, a clean suite passed 9/9, and the final exact-build
  runs passed 18/18 and 9/9 without retries.
- Browser viewports/devices checked: Playwright Chromium at 1280×720 and 375×812; in-app browser
  engineering inspection at the same two CSS viewports covered titin, PEVK, kinase, modeled force,
  value/object Sources filters, Close focus return, popup/stage-bar clearance, and document-level
  responsive overflow. No browser warnings or errors were logged. These are not a physical
  touch-device check and not human release-review evidence.
- Generated files regenerated: `index.html` regenerated; the 15-output release pack and its
  manifest/checksum regenerated and passed staleness validation.
- Release-gate fields changed, with evidence: the unauthorized dirty-tree SC-22 size checkpoint was
  removed and the governed SC-17-followup baseline (`ef0ce85`, 2,166,960 bytes, 20% tolerance) was
  restored. Whitespace-only bundle optimization reduced the current standalone to 1,987,197 bytes,
  below the unchanged 2,600,352-byte ceiling. The performance and final-release gates remain
  `PENDING`.
- Human-evidence pipeline status (SD reviewers, independent release reviewers, accessibility
  reviewer, learning-content reviewer, lay cohort, target hardware): owner-authorized
  citation-backed adjudication is recorded for the consumed decisions and is explicitly non-human.
  The pipeline still has no recruited independent release reviewers, accessibility reviewer,
  learning-content reviewer, or five-person lay cohort. No physical touch device was exercised;
  target-hardware and all later human release gates remain pending.
- Known limitations and next-sprint obligations: SC-23 must add scene filtering only after
  `scenes.json` exists and must not reinterpret SC-22's current value/object/chapter/all fallback.
  Independent claim-entailment, accessibility, lay-comprehension, physical-device, visual-matrix,
  deployment-parity, and final-release evidence remain future gates.

## Claim/source coverage count

The complete loaded registries contain **27 canonical claims**, **51 support relations**,
**65 references**, **12 annotation components**, **7 guided chapters**, and **7 expert cards**.
Every one of the 21 runtime annotations (12 components plus 9 titin regions) is exhaustively tested
so its visible ClaimView uses exactly the union of sources in its bound canonical claim records.

## Responsive inspector ownership matrix

| Audience state | 1280×720 | 375×812 |
| --- | --- | --- |
| Guided | Compact on-stage ClaimView: name, canonical plain statement, at most two compact citations, and object source route; bounded above the stage bar | Compact on-stage owner when pinned; bounded by the measured stage bar and mobile chapter surface |
| Evidence | On-stage inspector hidden; desktop drawer is the sole full ClaimView owner | On-stage inspector hidden; bounded bottom-sheet drawer is the sole full ClaimView owner |

Guided and Evidence ownership are both checked at 1280×720 and 375×812.

## Final verification

SC-22 is **COMPLETE**. Its acceptance list, bounded gate, generated-artifact checks, exhaustive
verification, routine browser suite, dedicated Evidence suite, and engineering browser inspection
all pass. This closes the sprint's implementation contract only; `release_ready` remains `false`
because the later independent human, physical-device, visual-matrix capture/review,
deployment-parity, and final-release gates remain pending.
