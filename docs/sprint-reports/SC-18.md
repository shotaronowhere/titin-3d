# SC-18 handoff

- Starting commit / ending commit: local baseline `ef0ce85`; deployed baseline `4375fee`;
  final candidate source `53a071e`; generated-output candidate `4e8583b`. This report and other
  post-candidate evidence do not alter candidate identity.
- Consumed decision IDs and fingerprints: no scientific decision was approved or consumed.
  Baseline ambiguous fingerprint `c079c03d56c4`; final model fingerprint
  `8234f36bad39a916b1428dc082c72dff0f7762d2b8404f50dbccc97eefe01cbe`; application revision
  `53a071e85d0df4d1d57af41617ec6f0a9f0d14e3`; build-input fingerprint
  `b2baf9914e9ca458864ad1f360f57c0c80f51235a64966ce4450f60f10d40605`.
- Status: **CODE_COMPLETE_BLOCKED_SCIENCE**. All code, bounded, exhaustive, identity, and routine
  browser gates pass. SC-19 is `NOT_READY` because no qualified SD-01–SD-05 reviewer has confirmed
  availability. The physical touch target and display connection path are also unconfirmed.
- User-visible changes: the page shows separate model/application/build-input identities; Evidence,
  Measure, and Sources open their labelled tabs and return focus; bibliography links and every
  extension-row state have explicit readable colours; region and close-up navigation no longer
  claims a wide view is active; missing WebGL points to the generated static fallback.
- Scientific/data changes and reviewer authority: no titin biology, force parameter, scope claim,
  or quantitative model input changed. Release truth moved to `titin-showcase-release-gates/2`, with
  scientific/human evidence retained as `PENDING`. No reviewer authority was invented.
- New or changed interfaces/schemas: `MODEL_INPUTS`; `modelFingerprint`,
  `inputManifestFromMetafile`, `buildInputsFingerprint`, `resolveAppRevision`, and boundary helpers;
  injected `Spec.identity` and `TitinVisualization.create(..., { identity })`;
  `titin-build-input-manifest/1`; `titin-release-manifest/2`; detached `MANIFEST.sha256`;
  `verify_artifact_identity.mjs --file --manifest [--url]`; Playwright helpers/configuration; release
  gate v2 protocols `titin-lay-comprehension/2` and `titin-expert-review/2`.
- Tests written first and observed failure: the baseline exhaustive gate passed (472 tests), showing
  that the old suite did not cover the defects. The new SC-18 test and browser harness were added
  with the implementation; a clean pre-implementation red run of the committed SC-18 test was not
  captured and is not claimed. During integration, the first exhaustive run correctly failed because
  Node discovered the Playwright spec; narrowing Node discovery to `test/*.test.js` created a new
  source candidate before regeneration. Initial browser failures also exposed two invalid test
  assumptions (a dynamic locator loop and toggling the already-selected PEVK row); those tests were
  corrected before evidence was accepted.
- Negative controls: artifact/evidence boundary 8/8; release-gate schema 25/25; integrated showcase
  release 7/7; all legacy structure, geometry, presentation, claim, and evidence destructive controls
  pass under `npm run verify`.
- Commands run and results: baseline `npm run verify` PASS (472 tests; 49.12 s); exact package and
  browser install PASS; `npm run build:release`, `npm run pack`, `npm run verify:identity`,
  `npm run check:build`, and `npm run check:pack` PASS; final `npm run verify` PASS (480 tests plus all
  validators/negative controls); `npm run verify:sc18` PASS (45 bounded tests plus validators and
  negative controls); `npm run test:browser` PASS (8/8 Chromium, 47.9 s).
- Browser viewports/devices checked: Playwright Chromium 151.0.7922.34 at 1280×720 and 375×812;
  standalone checked over loopback and `file://`; source mode checked over loopback. No physical touch
  device, display-path confirmation, Firefox/WebKit matrix, or manual target-display review is claimed.
- Generated files regenerated: `index.html`; `release/CLAIM_MATRIX.md`, `LIMITATIONS.md`,
  `PREFLIGHT.md`, `PRESENTER_SCRIPT.md`, `SCREENSHOT_PACK.md`, `MANIFEST.json`, and
  `MANIFEST.sha256`. The six fallback SVGs reproduced byte-for-byte and therefore did not change.
- Release-gate fields changed, with evidence: `artifact_identity` and `browser_qa` are `PASS` from
  the verifier, boundary validator, unit tests, and browser smoke. `automated`, `destructive_controls`,
  and `release_artifacts` remain supported `PASS`. Scientific decisions, claim entailment, mechanical
  validity, deployment parity, visual/human accessibility, performance target hardware, lay/expert
  review, rehearsal, and final release remain `PENDING`; `release_ready` remains `false`. The target
  baseline record is [`evidence/performance/SC-18-baseline.json`](../../evidence/performance/SC-18-baseline.json).
- Human-evidence pipeline status (SD reviewers, independent release reviewers, accessibility
  reviewer, learning-content reviewer, lay cohort, target hardware): every required role has a stable
  row in [`docs/human-evidence-pipeline.md`](../human-evidence-pipeline.md), but all are
  `NOT_CONTACTED`/`UNCONFIRMED`. The observable laptop/GPU/browser/display is named; touch device and
  connection path remain unassigned/unconfirmed.
- Known limitations and next-sprint obligations: recruit and confirm qualified SD-01–SD-05 reviewers
  before SC-19; confirm the integration-owner handle; name and exercise a physical touch device;
  confirm the display cable/adapter path; capture OS process-memory high-water and physical-display
  performance; retain post-candidate evidence outside every candidate input; run Firefox/WebKit and
  the full physical/manual matrix in SC-27; do not implement the unapproved 22-domain hypothesis.

## Baseline and after-state measurements

| Measurement | Baseline | SC-18 candidate |
|---|---|---|
| Local source / deployed source | `ef0ce85` / `4375fee` | app source `53a071e`; generated output `4e8583b` |
| Identity | one ambiguous data digest `c079c03d56c4` | model/app/build-input identities shown above |
| Actual `index.html` bytes | 2,166,960 | 2,178,874 (+11,914; +0.55%) |
| Actual `index.html` SHA-256 | `a03c9b2071ffd92ec0672cc7e51ce7abc2d7249ceca2c6b56440eb47610dec77` | `9d5fefbfdbd429906355ff1fa863c4feaca86ed932b675bf4736afcacc0b9fe1` |
| Manifest SHA-256 | `3000b08b2d057c5190c5a054d3e17ab3abf68f128a75f48e694ce8762a17ed33` | `b55ea0568cebebff8fbe4160849936d0da73c6458c220c26d135a186b2a19662` plus detached checksum |
| Manifest raw-byte truth | v1 declared 2,165,946 bytes, 1,014 fewer than the Git object | v2 records 2,178,874 bytes and the exact raw SHA-256; verifier passes |
| Automated browser evidence | none | Chromium 151; both named viewports; 8/8 smoke scenarios |
| Reproducible performance sample | none retained; no retrospective value invented | startup 1,088.259 ms; 120-frame average 24.828 fps; p95 50.1 ms; max 50.6 ms; Measure 519 ms; peak observed JS heap 14,300,000 bytes |

The performance sample is headless automation on the named laptop. It establishes a reproducible
SC-18 baseline but does not satisfy the pending physical target-hardware gate.
