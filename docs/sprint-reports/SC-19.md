# SC-19 handoff

- Starting commit / ending commit: started at `6a7b61bff1365ef3b1ae35a3503640c1da9a6d3b`; repository-controlled implementation ends at `f46a8b6440df89cc5d4fe79ab4b4f9e1537c80dd`. The handoff record and post-candidate inspection evidence are committed after that implementation commit and are intentionally outside candidate identity. The frozen candidate source commit is `3eb7dc54d1246aca59e9980ca7f65bd0d2f4b99a`; generated candidate outputs are committed at `638b09f6c2ba471d89cbeed5ec339131ec7df904`.
- Consumed decision IDs and fingerprints: no approved scientific decision was consumed. SD-01 through SD-05 are `PENDING`. Their byte-pinned packets are SD-01 `f8f48de862ac3f8786b1622398cff467dbbe2d22c3a9d49b1030902a34a31396` plus region report `31a293737079b5244d8f81eabcd5c1914b568ed1e338fe287bc8f8c6061031a5`; SD-02 `671a547bd3fceeeafd6f7a1002fba795220e6352a593d5d1deec23bfa85d7745`; SD-03 `e7c671b981a0386926d3e1e5f2799ebe78608451b751e677317dd77f42e61012`; SD-04 `af2d78b23bd26531cd0363b76b0e5e7dc51b759b07635c8985aba9afdcdfd42b`; SD-05 `a868b383592da89af486bee713dc84f40e851e65b25545ca346a77ae8ebb984c`. The frozen candidate identities are model `7c32782023041e9e4ced13b30828ffadda11f08e6b744d1a6857cfbb18a68d5c`, application revision `3eb7dc54d1246aca59e9980ca7f65bd0d2f4b99a`, and build inputs `dd3ee440b4a393a537b5686122815d890b9772b093042ee4e76ac3df23a000b3`.
- Status: **CODE_COMPLETE_BLOCKED_SCIENCE**. All repository-controlled SC-19 acceptance work is complete. Scientific completion is not claimed: no qualified reviewer is confirmed and no ruling exists. This is the sprint contract's required terminal status when prerequisites remain pending.
- User-visible changes: the public badge now renders only the normalized ledger wording, “Human TTN reference sequence · Q8WZ42-1 · construct review pending.” The authority card visibly reports `5 pending · 0 approved · 0 deferred`. Evidence mode exposes SD-01 through SD-05, the current claim's evidence class and limitations, exact source links, and explicit non-claims. The passive-force display states the rat-psoas transfer and pending SD-04 review adjacent to the output.
- Scientific/data changes and reviewer authority: `data/titin_sequence_features.json` pins the human UniProt Q8WZ42/Q8WZ42-1 feature export from release `2026_02`, entry version 221, sequence version 4, 34,350 aa, with upstream and sequence checksums, 285 `DOMAIN`, 24 `VAR_SEQ`, 297 `VARIANT`, and 71 `CONFLICT` features. The deterministic region report has no gap, overlap, omission, or multiple assignment. It preserves rather than resolves the two review questions: the current N2A interval declares one Ig-like domain but contains zero pinned `DOMAIN` features; the current distal-Ig interval declares 15 and contains 16. No region boundary, declared domain total, contour, geometry, force parameter, or curriculum ruling changed. `data/claim_support.json` has 27 atomic records: 13 `MEASURED`, 4 `STRONGLY INFERRED`, 3 `MODELED`, 1 `INFERRED`, 1 `SCHEMATIC`, and 5 `UNKNOWN`; every review is `PENDING`. Engineering verified schema, identity, binding, citation-registry closure, deterministic derivation, and visible presentation only. No engineer was treated as scientific authority.
- New or changed interfaces/schemas: added `titin-sequence-features/1`, `titin-scientific-scope/1`, `titin-claim-support/1`, and `titin-scientific-decisions/1` records; made all four mandatory `SPEC_FILES`; added sequence features to `MODEL_INPUTS`; and added scope, claim support, and decisions to build-input identity only. Added pure `ScientificScope` and `SequenceFeatures` model helpers and corresponding `Spec`/`TitinModel` access. Claim records require exact JSON/DOM public bindings, subject/preparation, source subject, locator, support relationship, extraction note, limitation, review payload digest, and named-human review fields. Scientific decision approval requires packet-byte and reviewed-payload digests plus reviewer and implementation verification metadata. The release pack gains `SCIENTIFIC_AUTHORITY.md`.
- Tests written first and observed failure: SC-19 tests and destructive controls were added before the feature was accepted, but a pristine preimplementation red transcript was not preserved, so this report does not invent one. During final integration, the bounded gate first rejected stale generated outputs; the semantic-unit destructive case exposed that “repeat” was not recognized as a length unit context; legacy SC-5/6/8 assertions still expected the removed skeletal-N2A fallback; and the phase-6 geometry validator still required duplicated species/isoform fields. Each observed failure was corrected in the authority layer, test expectation, or validator, then the complete gates were rerun.
- Negative controls: `scripts/neg_control_sc19.mjs` passes 30/30 destructive mutations. Coverage includes coordinate-frame and residue-boundary mutation, one-domain reassignment, upstream checksum/version drift, unregistered identifiers, missing or malformed locators, missing subject/preparation, wrong source subject, allowed-but-semantically-wrong units, unresolved JSON/DOM bindings, missing annotation binding, evidence-class promotion/demotion, a different valid source binding, stale claim approval/payload digest, stale decision packet/review payload/model fingerprint, fabricated or incomplete reviewer metadata, and scope fallback divergence. All other repository negative suites also pass under `npm run verify`.
- Commands run and results: required order was executed as build (`npm run build`) → pack (`npm run pack`) → bounded gate (`npm run verify:sc19`) → exhaustive gate (`npm run verify`) → browser smoke (`npm run test:browser`). Final results: `verify:sc19` PASS with 117/117 bounded Node tests, all SC-19 validators, generated-artifact staleness checks, and 30/30 SC-19 destructive controls; `verify` PASS with 489/489 Node tests, every negative suite and validator, the 52-cell visual-matrix integrity check, and the Python structure-pipeline smoke test; Chromium PASS with 9/9 browser tests. `git diff --check`, `npm run check:build`, and `npm run check:pack` are rerun after this handoff closes.
- Browser viewports/devices checked: Codex in-app Browser at exactly 1280×720 for the required scope/card and claim/source visual inspection; Playwright Chromium 151.0.7922.34 headless exercised the source page, loopback standalone, `file://` standalone, SC-19 evidence surface, 1280×720 desktop routing, 375×812 responsive routing, declared foregrounds, navigation state, WebGL failure fallback, and pinned axe WCAG A/AA foundation. No physical touch device, projector route, or human accessibility review was performed; those later release gates remain `PENDING`.
- Generated files regenerated: `index.html` (2,486,093 bytes, SHA-256 `42baaed3f328f1bfaac3406a90fd5d6862d0c3933ad1aef70ba835e8bd5b51a9`), `release/MANIFEST.json`, detached `release/MANIFEST.sha256` (`e746a3350a4547149a2ecda963a367319e7e792b6de52d5e8ad9d3ba853c05b7`), `release/SCIENTIFIC_AUTHORITY.md`, the remaining release-pack Markdown, `docs/scientific-decisions/SC-19/SD-01.md` through `SD-05.md`, and `docs/scientific-decisions/SC-19/region-feature-report.json`. `npm run check:build` and `npm run check:pack` prove the committed versions are current.
- Release-gate fields changed, with evidence: `scientific_decisions` remains `PENDING` with SD-01 through SD-05 explicitly listed; `claim_entailment.atomic_claim_support` now records the 27-record matrix but remains `PENDING`; `release_ready` remains `false`. No screenshot was promoted to scientific or release-review evidence. Post-candidate integration evidence is `evidence/scientific/SC-19/inspection.json`, `authority-1280x720.png` (SHA-256 `eac73e30a3233e3d561ac8a6fdc8ffde1c5d8f0f656653bba0763a7eb43cbc61`), and `claim-source-1280x720.png` (SHA-256 `a4ecc4d2a9a0974c26021082cd0bd5c57757f9a0faa416cf6da91610d97f3570`). Its disposition is only `PASS_FOR_SC19_INTEGRATION_INSPECTION`.
- Human-evidence pipeline status (SD reviewers, independent release reviewers, accessibility reviewer, learning-content reviewer, lay cohort, target hardware): all five SD reviewer roles are `NOT_CONTACTED` / `UNCONFIRMED`; both independent SC-27 release reviewers, the accessibility reviewer, learning-content reviewer, and five lay participants are also `NOT_CONTACTED` / `UNCONFIRMED`. Independence/conflicts are `UNASSESSED` and publication consent is `NOT_REQUESTED`. The target laptop/headless browser baseline exists, but the physical touch device is `UNASSIGNED`, the display connection path is `UNCONFIRMED`, and physical display/browser/memory evidence is still absent.
- Known limitations and next-sprint obligations: the authority layer deliberately does not adjudicate biological correctness. A qualified specialist must independently open the exact locators, disclose conflicts, consent to publication, review the byte-pinned packet/model identity, and record a bounded ruling. SC-20 must not begin until SD-01, SD-02, SD-03, and its relevant SD-05 ruling are consumable. SC-21 remains blocked until SD-04 is consumable. Later SC-22/SC-27 human entailment, accessibility, visual, lay-comprehension, performance, deployment-parity, and rehearsal gates remain unresolved. A changed packet, source binding, locator, subject/preparation, reviewed payload, or candidate fingerprint invalidates stale approval and must fail closed.

## Exit determination

SC-19 is fully complete only in the contractually defined `CODE_COMPLETE_BLOCKED_SCIENCE` state.
The repository and integration work are complete and verified; scientific rulings are not. Starting
SC-20 or SC-21 as though the pending decisions were approvals would violate this handoff.
