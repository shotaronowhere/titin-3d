# Final presentation pass — September 9, 2026

The owner authorized this bounded finishing pass after an independent Claude review and a source/runtime critique. Baseline application: `dcdb403a759bd3df1f7d7bc821f3a73aeeb10caa`; baseline repository HEAD: `87e7942`. The prior ZIP and its evidence remain unchanged.

Final application source: `5d5f02d1a8699579cc775103c0cba793619aaa57`. The first candidate was `66c3c359bc84c581b122814c46008d1ad320f1ba`; its verification and captures are preserved in `prior-66c3c35/`. Test-only commits updated historical locator expectations without changing the application identity.

## Retained scope

- A drawer-sized force SVG, readable ticks, and wrapping HTML axis titles. The same model data, support regimes, sensitivity, units and source actions are retained.
- Large type enlarges Research prose, tables and force-chart labels. It no longer promises projector certification. Mechanics contribution labels wrap; the detailed parameter audit has a keyboard-accessible horizontal scroll region. Metrics wrap, and the smallest phone tab row fits.
- The secondary locator withdraws when the projected termini are too close to its lane. A conservative 48 CSS-pixel visual allowance protects nearby filament context. This is presentation clearance, not a physical measurement or an exhaustive mesh-occlusion audit. Main-model termini and applicable band spans remain. No camera or molecular geometry changed.
- Beat 2 explains that pink beyond the M-line belongs to the opposite half's separate representative path. The explanation appears only when that beat has mirroring enabled. Canonical scientific/presentation data records remain unchanged.
- Scientific source records precede build identifiers. All identifiers and existing source filters, exports and navigation remain available.

The active model, data, dependencies, anatomy, lighting, palette, and camera implementation are frozen. Header identity cleanup and broader composition work remain deferred. `release_ready` remains false. The intended delivery is a researcher-feedback preview, not independent biological validation or a demonstrated human learning outcome.

## Verification record

Development logs record actual outcomes, including the first two focused runs that exposed table/tab overflow and prompted the responsive corrections. The first targeted unit run passed 42 tests. Final-candidate verification, captures, reproduction, exports and packaging are recorded separately; development runs are not substituted for final evidence.

The combined development run passed 18 of 19 checks and caught an I-band-label withdrawal at 2000 nm: the secondary rail was correctly suppressed, but the long main-model caption did not fit the shortest spring span. The main span now uses the compact “I-band” wording at that length. Its label/geometry assertions were preserved; no timeout or numeric threshold was weakened to pass the failure.

The Stretch test now accepts either the existing locator's “I-band” or the main-model “I-band · extensible” span. It still requires a visible I-band label and checks the geometry inside the unobscured stage. This matches the intended secondary-locator withdrawal without removing the teaching or geometry assertions.

Use `PACKAGE.md` for the final identity and completed checks. Chromium plus Firefox is the bounded browser coverage target. WebKit remains unverified under the existing documented two-engine fallback; no repeated stalled WebKit matrix is planned for this presentation-only pass. A live presentation requires rehearsal on its actual hardware/browser/display.

The first full verification run passed 617/618 Node tests and stopped at a static source assertion requiring unconditional locator visibility. That obsolete requirement conflicts with the authorized collision fix. The test now requires the guarded locator condition and declared suppression state; the new browser suite independently checks visibility, retained termini and band labels, and the phone locator. The failed log is preserved as `development-verify-stale-locator-test.log`.

All 24 first-candidate desktop/phone screenshots were visually inspected, recorded in `prior-66c3c35/visual-review.json`. A clean detached worktree at `30e5ecc` reproduced all 23 first-candidate application/release files byte-for-byte, recorded in `prior-66c3c35/reproduction.json`.

The broad Chromium run passed 89/92 cases. The three failures were camera tests still requiring a locator in the finale at 375×812, 1024×768 and 1280×720, where the authorized presentation rule intentionally withdraws it. Screenshots and traces are preserved in `development-camera-failures/`. Test-only commit `2452dfd` explicitly expects suppression in these three layouts and visibility in the other reviewed layouts. It preserves every main-model projection, reachability, chrome-clearance and collision check, and retains locator-coordinate/vocabulary checks wherever the rail is visible. All six camera cases are rerun, rather than deleting the failed cases or weakening timeouts. The remaining 86 unaffected cases plus the six rerun cases form the 92-case coverage set; overlapping passes must not be counted twice. Application bytes and identity remain unchanged.

All six camera reruns and both keyboard/touch cases passed on `66c3c35`. Firefox then passed 37/41 checks and exposed actual enlarged-chart label intersections in four checks. These failures are preserved in `development-firefox-chart-failures/`. Source `5d5f02d` adds 8 viewBox units below the unchanged plot area, lowers x-label baselines by 6 units, and draws five x ticks instead of six. Numerical ranges, curve data, y ticks and force mechanics are unchanged.

The corrected candidate is verified with the full repository gate and 63 targeted browser cases: seven final-polish checks per engine; 24 Chromium evidence/preview/workbench cases; six Chromium camera cases; two Chromium keyboard/touch cases; and 17 Firefox evidence/preview cases. The preceding broader Tour, Stretch, viewport, history and accessibility results remain useful regression evidence for the prior candidate, but are not represented as fresh tests on the corrected bytes. `run_corrected_candidate.mjs` records the final outcomes. This avoids another full long matrix after a narrow tick-spacing correction while rerunning the affected surfaces and exact-byte packaging checks.

The corrected candidate's seven finishing checks passed in each engine. A second clean detached worktree at `6d75924` reproduced all 23 corrected application/release files byte-for-byte; see the current `reproduction.json`.

Final outcomes: all 63 selected browser checks and the full 618-test repository verification pass on `5d5f02d`. The new ZIP verifies, all 24 extracted walkthrough frames were visually inspected, and all 20 extracted exports pass. `PACKAGE.md` binds these outcomes to the delivered bytes.
