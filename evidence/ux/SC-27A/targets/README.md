# SC-27A target-frame contract

The approved target is the information architecture and frame contract in
`docs/superpowers/specs/2026-08-24-sc27a-ux-overhaul-design.md`: five Tour beats, no persistent
stage bar, at most four cold-open Guided affordances, contextual mechanics only in beat 3, and
Research as a bounded desktop drawer or full-screen mobile sheet.

No separate human target-frame disposition was collected in this implementation session. The
automated final captures are software-composition candidates only; human visual review remains
`PENDING` and no image in this directory is release evidence.

Use `DISPOSITION-TEMPLATE.md` to record the real review and the owner's explicit response to the
missed pre-implementation checkpoint. A later review can assess the implemented frames, but it must
not be relabeled as having occurred before implementation.
